{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build: vector_add",
            "type": "shell",
            "command": "nvcc",
            "args": [
                "-g",
                "-G",
                "${workspaceFolder}/sample/vector_add.cu",
                "-o",
                "${workspaceFolder}/sample/vector_add"
            ],
            "group": { "kind": "build", "isDefault": true }
        }
    ]
}